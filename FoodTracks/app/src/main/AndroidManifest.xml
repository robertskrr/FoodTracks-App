<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodTracks">
        <activity
            android:name=".gui.users.local.PerfilLocalActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.cliente.PerfilClienteActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.admin.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.local.DashBoardLocalActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.cliente.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.local.RegisterLocalActivity"
            android:exported="false" />
        <activity
            android:name=".gui.users.cliente.RegisterClienteActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodTracks" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FoodTracks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>